# 剩余302项逐项复测

首轮已完成302/302，执行并发为1。每项实际运行，正常存活的项目追加60秒虚拟时间持续检查。302项已逐项复核截图，不能将此计作302项兼容通过。

原始结果：`artifacts/compat-20260914/pending302-first/results.json`。131项需要场景复核，83项默认操作未改变画面，55项退出，28项运行错误，4项黑屏，1项解析失败。具体截图、报错、处置均在`review.json`，原始文件未改写。

后续仍串行运行定向输入。`targeted-scenes.json`用于菜单、触摸教学和工具功能补测。安装器补测在`pending302-installers`，已确认非空文件输出；工具新增文件大小和SHA256证据，不凭完成文字判断写入成功。退出并不必然是运行时缺陷：若输入选中退出菜单，记录入口重试。

花屏按用户要求暂缓。所有未证实玩法/功能完成的项保持resolved=false；网络服务、缺失包、父平台依赖另行记录。每个确认修复的独立部分经验证后提交Git。
