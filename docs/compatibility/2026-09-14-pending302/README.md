# 剩余302项逐项复测

## 已执行范围

首轮完成302/302，执行并发为1。每项独立启动及输入，正常存活的项目追加60秒虚拟时间持续检查。302项已逐项复核截图，不能将此计作302项兼容通过。

原始结果：`artifacts/compat-20260914/pending302-first/results.json`。131项需要场景复核、83项默认操作未改变画面、55项退出、28项运行错误、4项黑屏、1项解析失败。具体截图、原始报错和处置均在`review.json`，原始结果未改写。

首轮后另有23项及14项定向操作补测、4项安装器输出核验、5项已审阅场景独立重跑。所有游戏测试均串行。菜单退出不是自动认定运行时缺陷；触摸应用以实际按钮和滑杆位置补测。

## 功能验证

本轮8项功能验证通过：sky插件V7安装包、sky字库V3安装包、vmrp安装包、调色板XL，以及四个趣拼版本（商店ID1628/1629/1630/1631）。安装器输出不只看完成提示，还逐项核对文件大小和SHA256：V7六份输出、vmrp十一份输出与包内资源相同；V3的1179630字节字库与24段资源顺序合并结果相同。

调色板RGB由0拖动至201后色值和颜色更新；四个拼图版本移动图块并增加步数，持续60秒。`verified-scenes.json`固化已审阅画面，独立回归五项全部passed。原313项累计验证19项，仍有294项未达到完整功能结案标准。

## 启动预算修复

三个应用的同步资源释放不是无限循环：#523需要128056648条指令、#726需要168928009条、#1350需要131326022条，均超过旧128M预算。独立诊断采用256M显式预算，三个启动调用正常返回；紧接着的输入/定时器调用只需几千指令。证据为`budget-diagnosis.json`，原始CPU/ABI调用记录在`artifacts/compat-20260914/pending302-diagnostics`。

默认与最大预算改为256M，保留显式小预算和单调时钟30秒截止时间。商店测试器使用同一个生产默认值，避免继续硬编码旧预算。`pending302-budgetfix`验证三个应用启动并持续运行，进入菜单或应用画面。#727仍在256M预算处失败；#2405触发30秒截止时间，均未标记修复。启动修复不等同完整玩法结案。

## 验证与限制

860项测试通过、1项跳过，包含显式64指令无限循环保护和低预算解压测试。类型检查、EdgeOne构建及ZIP CRC验证通过；未部署。

16项既有功能场景的串行回归全部passed，使用`budget-regression-manifest.json`和`budget-regression-scenes.json`，结果在`artifacts/compat-20260914/pending302-budget-regression/results.json`。

花屏按用户要求暂缓。联网服务失败、缺失下载包、父平台依赖、未知故障均保留具体证据，不将其记作兼容通过。`review.json`中的resolved只表示已有实际功能证据的8项，其余294项保留待修复/验证状态。
