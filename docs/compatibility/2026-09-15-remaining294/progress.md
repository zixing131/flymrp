# 294项后续修复：目录枚举

## 软件魔盒 #1300

旧实现省略目录的`.`和`..`。空目录findStart返回有效句柄和空文件名；软件魔盒把空名称当作子目录递归，构造`appbox/plug////...`，最终指针被0x2f字节覆盖并崩溃。

原生依据：本地rxgj-main的src/file_lib.c:375直接返回系统readdir结果，native_dsm_funcs.c:251转发该名称，mythroad/dsm.c:1646打开目录并取首项。原生包含`.`与`..`，空目录不会把空字符串作为首项。

现在ARM和Lua的find接口共享AppFileSystem.findEntries，包含点目录项；普通文件列表保持只返回真实子项。对应空目录、GBK文件名和Lua遍历测试更新。隔离对照和正式代码均不再触发非法指针，写出8个appbox文件后退出安装流程。主功能尚在验证，不以安装退出当完整通过。

860项测试通过、1项跳过；16项既有功能场景串行回归全部passed；类型检查通过。真实证据目录：artifacts/compat-20260915/appbox-fixed、directory-regression。

## 明星美女麻将馆 #1410

已定位UCS2写溢出：guest在0x1e93f1c转换字符串，首次计数遇到NUL后申请24字节；写入循环遇到非法高位字节后跳过后继NUL，继续读写，覆盖0x2a4b40空闲块。调用者0x1e9c36c把一段大端UCS2说明文本当GBK传给文字测量函数。当前证据指向guest内的编码/边界问题，未通过扩大堆或忽略堆校验掩盖；尚未结案。CPU、寄存器和内存写入记录见artifacts/compat-20260915/diagnostics/heap-1410.json。

## 功能补测

12项第一轮定向操作和11项第二轮均串行完成。#1040节日祝福语验证连续下一条/上一条导航，#2486找回元素完成第一关并进入第二关；已审阅画面独立回归均passed并持续60秒。原313项累计结案21项，剩余292项。软件魔盒主功能仍待验证，其安装流程修复未计入结案。

## 父目录路径与后续验证

点目录项需要真实可访问。补齐路径中的`.`/`..`折叠，目录info对工作目录返回MR_IS_DIR，保留`.hidden`字面名称。原生get_filename（dsm.c:1053）对空名称拼接dsmWorkPath，因此空名称也是目录而非不存在的文件。新增父目录访问、重命名和ABI目录类型测试。862项测试通过、1项跳过。

软件魔盒的安装后主包为appbox/plug/opxrjmh.mrp。在保留安装文件的新运行时中启动它，可进入更多软件/精品软件/我的软件及菜单。在线目录请求proxy.51mrp.com/page2未匹配本地离线资源，持续重试；未伪造服务数据，因此在线功能未结案。完整运行1785 ticks、115104ms，无运行异常；证据appbox-child、appbox-enter、appbox-tabs和diagnostics/appbox-installed-files.json。

合并原302项与此前11项为313项串行回归，使用regression313-manifest.json和regression313-scenes.json。补入已核实的按键/触屏操作，仍以每项实际功能证据结案；运行结果写入artifacts/compat-20260915/regression313。

## 全313项串行回归及RAM工具

313/313运行完成，18项已有场景断言全部passed；原3项安装器仍以输出文件证据验证。其余135项needs-scene-review、77项no-input-response、56项exited、24项runtime-error、2项black-screen、1项损坏包解析worker-error，均未凭分类自动结案。完整汇总regression313-review.json。

ram_scan #192补测完成4MiB dump文件写入、返回工具首页及8MiB扫描至100% All memset OK；followup3实画面复核，followup4独立回归passed并持续60秒。累计22项验证，剩余291项。

象棋 #18正确落子后触发电脑思考阶段256M指令预算错误，现场followup3/7-failure.png，仍未结案。夜袭画面变化目前来自动画，未验证弹药消耗，不计通过。

## 找牌及金币收集

偷看美女 #2036完成洗牌、开牌、找出不同牌，出现成功提示；要钱不要命 #2342完成重新开始、左右移动和起跳，控制后12金币151分，持续后19金币277分。followup4实际画面复核，followup5独立哈希回归两项passed。累计24项验证，剩余289项。

## 连续读取兼容模式

两份MTK魔法寿司的资源宽度位于奇地址（例如0x1eb27c5），内容为28。默认ARMv5 LDR旋转得到0x0700001c，引起绘图循环越界。新增可选wordLoadMode=bytewise，仅影响LDR，保留默认ARMv5与既有存储/SWP语义；播放器提供全局及单游戏内存读取设置，重启生效。正式模式两份包都进入正常棋盘；sushi-play已看到三连消除和补充棋盘。21项既有功能回归全部passed，873单测通过、1跳过，类型检查和构建通过。

用户最新要求改为只验证能进入游戏，不再验证过关/得分/持续游玩。已停止sushi-verified后续持续验证（其22项结果已passed，23项中止，不按失败计）；后续采用正常主界面/游戏画面入口标准，历史完整功能证据保留。

*G/*J诊断：18、65、126三例无table127注册调用（后两例各266次ABI调用均未注册）；包内无对应M0内容，仍是缺少宿主预装资源，不能凭空补空包。

## 按最新要求改为启动入口验收

不再跑得分、过关或60秒玩法，正常主菜单、游戏画面或工具主页出现就继续下一项。加载、网络错误和黑屏不算通过，花屏问题暂缓。当前入口确认 193 / 313，剩余 120 项保留具体阻塞或待确认原因，详见 entry-review.json；此前24项完整功能验证仅保留为历史记录。

新增 MRP_TEST_MODE=entry，串行完成174项入口补测及短确认操作。入口模式输出 entry-review，截图仍需人工判断，非黑画面不自动算通过。

提交 8f4d55e 修复 B 盘空间误报及可选平台接口106：三款冒泡（testId 4、145、148）均已进入首页。873项测试通过、1项跳过，类型检查与生产构建通过；证据 artifacts/compat-20260915/storage-fixed/results.json。

毒蛇行动2（testId80）默认ARMv5模式，短等待并确认音乐提示后进入开始游戏菜单；未运行玩法。46、96、270的连续读取模式补测仍分别为堆损坏、预算超限、执行超时，保持未通过。

## NUL文件名及键盘映射查询

应用列表8.0（testId6）的start.mr加载`logo.ext\0\0\0\0\0`。原生mythroad.c:3600/3610将_strCom 601/602的参数传给接收C字符串的_mr_readFile；现修复两接口在首个NUL终止文件名，保留返回资源中的二进制NUL。随后出现1210（mrporting.h:682，MR_GET_KEYPAD_MAP），遵循dsm.c默认MR_IGNORE且不写输出，成功进入文件/应用列表主页，未继续使用功能。

875项单测通过、1跳过；类型检查及生产构建通过。真实入口证据artifacts/compat-20260915/nul-keymap-entry/6-entry.png。13/105首个启动脚本引用包内缺失的mrc_loader.ext，诊断保留；81加载器另查plugins/dump0..7均缺失，未以替换启动脚本掩盖。

## 剩余134项入口回归完成

01a8240后串行完成134/134：101项entry-review（需要人工判断）、18运行异常、12主动退出、2黑屏、1损坏包。与此前同场景输出比较，没有新的入口画面变化；未将entry-review自动升级为通过。完整结果artifacts/compat-20260915/remaining134/results.json。

听听音阅testId5已显示主页和三个内容页签，按用户最新入口标准结案；未注册、无内容限制明确保留。给力猫testId98短等待及确认后仍无已确认正常完整入口，不计通过。累计180入口确认、剩余133（包含按用户要求暂缓的花屏项276）。

## 延长启动复测

手机UC原版（testId208）在500 ticks后进入 UC 浏览器主页，显示书签、新闻、贴吧、个人和菜单；按入口标准结案，未访问网络内容。其余黑屏、安装或“正在加载”项目的同样复测没有出现本地菜单；不将宣传或安装页算作通过。累计181入口确认、剩余132。

注册提示的13项仅发送一次“取消/返回”后，均进入我爱冒泡的“服务器连接失败，请重试”壳层或继续内存检测，没有离线内容主页。14项加载页延长至200 ticks后也仍是安装、宣传或网络加载页；5项机型拒绝改用 Nokia/N73 身份后仍明确提示“不支持此类应用”。这些场景文件保留为可复核的负向证据。

## 静态页确认键补测

便宜！绝对划算、两份捕鱼达人、疯狂捕鱼、给力猫和应用软件>>各只发送一次确认键。两份捕鱼达人、疯狂捕鱼和应用软件>>无画面变化；便宜！绝对划算和给力猫仅切换安装宣传动画，未出现本地功能页。结果保存在 `artifacts/compat-20260915/static-confirmation/results.json`，均维持未通过。

## 旧宿主父启动上下文

15个包的启动脚本以 `_nes3ShP7SwK0 == 370` 明确拒绝“direct”启动。运行时现在在执行 `start.mr` 前提供该旧宿主固定标记；这不是账号、网络或付费状态。复测后，MSN、QQ2010、QQ网游大厅、大师在线签名、火车订票、手机QQ、途客行天下和英汉字典等12项进入登录页或本地功能主页。QQ斗地主仍报告 MRP 版本错误，飞信仍停在网络登录加载，来电助手仍要求运营商号码更新，因此未计入。累计193入口确认、剩余120。
