# 第四轮：313 项逐项复查（进行中）

目标尚未完成。原第三轮剩余 313 项被冻结在 `2026-09-14-goal-313.json`；短冒烟通过不等于可玩，注册页、下载页和静态画面不作为结案证据。另保留 #603 的持续运行修复及验证记录（见后文）。

## 已实施的修复

- 补齐真实共享组件 `msbase.mrp`、`mscore.mrp`、`kbill.mrp`、`addons.mrp`，在生产资源清单及离线下载映射中登记。来源、大小、SHA256 见 `assets/README.md` 与 manifest。安装链已能取得 499990、499991、480292，后续仍请求缺失的 480101，目标文件 `MNS/fish.bmp`，不能把下载页算作游戏成功。
- `_bmpInfo` 返回真实共享像素内存地址及长度、尺寸、类型；Lua 位图与 native 像素双向共享。补齐可忽略的平台查询 1210、1221、2200。#160 NES 已从 ABI 异常推进到“未授权”页面，仍未解决。
- 默认模拟器制造商标识使用 `sdk`，保留显式 profile 覆盖。7 个原 `sdk_key.dat` 路径错误样本不再异常，但其中仍有资源下载、联网及输入流程待核实。没有生成或伪造设备密钥。
- 补齐内部数据表 table[23] 的生命周期指针，扩大 table[23]/[24] 的独立分配尺寸。同步 guest 写入的运行状态、定时器状态、回调和暂停策略；原生 restart 使用 guest 发布的 table[100]/[101] 包名及入口，由既有定时器执行。

## 原生重启根因与验证

受控样本 #253 使用冻结商店 manifest 的精确包。修复前仅调用一次定时器，在工作缓冲绘制“正在启动…”但不提交画面。独立临时探针证明 guest 写入内部状态 3（RESTART），宿主仍为 1（RUN）；此前内部表没有对应指针，状态写入未被宿主接收。

修复后 #253 执行子包重启并提交界面。原 33 个黑屏样本按相同入口、控制、60 秒后续虚拟运行重测：29 个 `needs-scene-review`，2 个退出（#1143、#1929），2 个仍黑屏（#1883、#2360），无 runtime-error。29 个并不等于 29 个已修复游戏：多个样本落在“我爱冒泡”的注册询问页。完整输入、帧指纹与 PNG 见 `artifacts/compat-20260914/round4-black-lifecycle/`。

参考实现的 `gwyaz-startup-hang-progress.md` 提示过类似生命周期问题；本仓库的 table[42] 本已区分 EFS 与安装源，本次没有添加文件存在伪装或自动呈现画面的补丁。

## 回归状态

- 生命周期补丁前，包含共享资源、位图及 sdk 标识的全量 2640 项：2378 短冒烟通过、123 静态、56 退出、50 runtime-error、33 黑屏。相较第三轮 2327 个通过增加 51，原通过项无回退。产物 `artifacts/compat-20260914/round4-full-sdk/results.json`。
- 生命周期补丁后单元：154 文件通过、1 跳过；830 测试通过、1 跳过。新增测试覆盖 guest restart 不重复重启定时器、host 定时器到期不被旧 guest 状态复活、暂停期间定时器运行。
- 内部数据表扩容使固定取证地址平移：数据/guest heap +312，后续 EXT 分配 +352。相应取证常量与断言按实际布局更新，未删除执行路径、字节内容或返回值校验。
- 生命周期补丁后的全量回归目录：`artifacts/compat-20260914/round4-full-lifecycle/`，已执行完成，结果见后文。
- 已目视复核的 5 个静态样本已在当前代码重放验证：#266、#267、#465、#1018、#1024。配置 `round4-zero-reviewed-scenarios.json` 固定了已审核游戏画面指纹，产物 `round4-zero-reviewed/`；只有当前代码复现通过才记录已核实。

## 仍需继续

安装链 480101、注册与联网依赖、2 个剩余黑屏、退出类真正入口、SDK 样本后续流程、其余静态样本的触摸/按键及持续运行、runtime-error 根因，以及原通过项的回归差异。音频上一轮 20 项 IMA ADPCM 与 FFmpeg 一致的验证仍保留；本轮没有以该结果替代其余游戏的实际发声验证。

## 后续增量：原生入口与俄罗斯方块

生命周期全量回归完成：2409 短冒烟通过、114 静态、61 退出、50 runtime-error、6 黑屏。相较前一 SDK 快照有 4 项分类变差（#135、#234、#632、#2390），因此不能宣称该快照无回退。#135/#2390 是应用列表启动所选子应用后退出；#632 实际进入“手机不支持拍照”页；#234 安装后指定 `cfunction.ext` 为重启入口，被错误交给 Lua。后续均继续追踪。

### DSM 原生入口

参考 `mythroad_mini.c:1792` 的 `mr_doExt` 路径，添加明确 `.ext` 入口及缺少 Lua 入口但存在 `cfunction.ext` 的原生启动：EXT load、code 6、code 8 应用信息、code 0，并在原生模式转发 pause/resume。没有补写或替换游戏脚本，没有忽略损坏 Lua 入口。3 个新增测试覆盖原生启动生命周期、显式原生重启、显式 Lua 入口错误保留。

#234 已从错误进入 XQC 编辑器界面，输入功能仍待验证。11 个缺 `start.mr` 样本的持续诊断有 10 个无异常，1 个按键退出；随后的画面重测确认“大漠狂刀”实际战斗、“冒险岛”剧情及其他游戏菜单。入口恢复不等于全部可玩，详情保留在 `round4-native-entry-scenes/`、`round4-native-touch/`。

独立的入口恢复实验只用于核对来源：多组同版本包的 `cfunction.ext` SHA256 均为 `13100cb331c7d409c320a0a9f4d432d8c54f51b5c58175ea6fcfc9253c9495d6`，原启动脚本有两种固定指纹；正式实现采用 DSM 原生入口，并未采用脚本移植。比较证据 `round4-entry-recovery-hashes.jsonl`。

### #603 持续越界已修复

`mythroad.c:2320` 为地图申请 `w*h*2` 字节，而 `mem.c:26,57` 的 `realLGmemSize` 按 8 字节对齐。11×22 地图逻辑 484 字节、实际 488 字节；出错坐标 `(11,21)` 对应字节偏移 484，位于分配尾部对齐空间。SetTile/GetTile 原调试边界使用 `>`，不是 `>=`。

实现保留地图实际分配空间，按逻辑尺寸绘制/移动地图，同时拒绝超过坐标兼容边界或真实分配范围的读写。TileShift 不把对齐字节移动进可见地图；同尺寸 TileSet 保留存储。新增测试覆盖尾部写入、重复设置、移动不污染、分配边界与坐标越界拒绝。

`round4-tile-reviewed/results.json`：`passed`，2487 ticks（198.96 秒虚拟时间），9 次控制变化，游戏场景和交互指纹均匹配，持续运行无异常。#603 在目标台账中单独标记解决，不计入原 313 项的分母。

当前单元 834 通过、1 跳过。最新全量目录 `round4-full-native/` 已完成。原 5 个触摸/按键样本 `round4-zero-reviewed/` 已全部复现 passed，全量回归已完成，但全局仍有 3 项分类回退，目标不结案。

480101 的补充查找覆盖两本地目录全部 33117 个普通文件（不限扩展名），没有 appid 命中，也没有 MNS/fish.bmp 路径命中。不能使用其他游戏的同名鱼精灵替代它。

## 提交前状态核对

`round4-full-native/results.json` 完整 2640 项：2420 短冒烟通过、114 静态、62 退出、38 runtime-error、6 黑屏。原第三轮未通过的 313 项中，96 项当前短冒烟通过；这不是已解决数。原通过项有 3 项分类回退：#135、#2390 退出，#632 静态，仍需继续验证和修复。

2026-09-14 工作目录迁移到 `flymrp` 后，全套单元测试 844 通过、1 跳过，类型检查及 EdgeOne 构建通过。没有因为迁移、上传或 Git 提交而缩小兼容性目标。
